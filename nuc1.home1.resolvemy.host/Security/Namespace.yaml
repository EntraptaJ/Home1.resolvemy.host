apiVersion: v1
kind: Namespace
metadata:
  name: cert-manager

  annotations:
    argocd.argoproj.io/hook: PreSync

  labels:
    kubernetes.io/metadata.name: cert-manager

spec:
  finalizers:
    - kubernetes